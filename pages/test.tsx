import { Layout } from 'components';
import { Landing } from 'components';
import type { NextPage } from 'next';

const Test: NextPage = () => {
    return (
        <Layout>
            <Landing />
        </Layout>
    );
};

export default Test;
